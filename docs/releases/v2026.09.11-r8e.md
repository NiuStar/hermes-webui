# v2026.09.11-r8e

User-accepted application trial and long-session completion release.

- Application task/approval wiring and bounded storage ownership implementation.
- Separate metadata/display message count coordinates; retain existing content on failed refresh.
- Normal completion can send a notification-only payload (`HERMES_COMPLETION_NOTICE=1`). Preserve streamed content until page reload, then load the latest 30 visible messages with associated tool records.
- Settle the final streamed node without hiding it, duplicating its activity projection, or leaving a live cursor. Projection retirement uses stream and segment identity.
- Bounded opt-in timing diagnostics (`HERMES_TIMING_PROBE=1`).

## Validation boundaries

User reported acceptance. Latest affected/neighboring regression: 448 passed, 11 subtests passed, three existing compatibility warnings. Desktop/mobile live long-history tests cover plain replies, tool plus multi-paragraph replies, retained content, one visible final copy, zero final live cursor owners and reload recovery. Earlier full applicable regression predates final frontend changes; it is not current whole-repository evidence. Independent tooling review previously timed out and is not claimed as approval. Remaining backend completion latency is not claimed resolved.

## Artifact

Git release tag: `v2026.09.11-r8e`.
Docker tags: `24802117/hermes-webui:v2026.09.11-r8e`, `24802117/hermes-webui:latest`.
Accepted source image: `sha256:121dde4d495c9f71496381f7efebf4a14581339d8d89707c3d60b1c8bb56df26` (linux/amd64).
Embedded UI build version: `trial-notice-r8e`; the release retags the accepted artifact without rebuilding it.

Image contains runtime code, not the trial's mounted conversations/configuration. Deploy with explicit isolated persistent mounts and configuration. Publication does not replace existing deployments. Registry manifest digest must be read back after push; it can differ from a local image/index identifier.
